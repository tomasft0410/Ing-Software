<%= render 'users/shared/up'%>

<section class="hero is-fullheight is-link">
  <div class="hero">

<h1 class="title is-1">CONTRATOS</h1>

<div class="columns is-multiline">
    <% @contratos.each do |contrato|%>
        <% if current_user %>
        <div class="column is-one-quarter">
        <div class="card">
        <div class="card-image">
        <figure class="image is-240x240">
        <img src="<%= carrete.imagen %>" alt="Placeholder image">
        </figure>
        </div>
        <div class="card-content">
      <div class="media">
        <div class="media-content">
         <p class="title is-4" style="color: black"><em><%= carrete.titulo %></em></p>
          </div>
      </div>
      <%= link_to 'Ver detalle', carrete_seleccionado_path(carrete.id) %>
          </div>
  </div>
    </div>
        </tr>
        <% end %>
    <% end %>
<% if current_user %>
<% else%>
<p>a No estas conectado, entonces no puedes ver los Carretes </p>
<% end %>

</div>
<% if current_user %>
<a href="new.html" class="button is-link">Crear Nuevo Carrete </a>
<% end %>
<a href="../welcome/index.html" class="button is-link">Volver al Inicio </a>
  </div>
</section>