<%= render 'users/shared/up'%>

<p id='notice'><%= notice %></p>
<div class="columns is-multiline">
        <div class="column is-one-quarter">
        <div class="card">
        <div class="card-image">
        </div>
        <div class="card-content">
      <div class="media">
        <div class="media-content">
         <p class="title is-2" style="color: black"><em><%= @comentario_carrete.carrete.titulo %></em></p>
         <p> Contenido: </p>
         <p class="title is-4" style="color: black"><em><%= @comentario_carrete.contenido %></em></p>
         <p> Fecha: </p>
         <p class="title is-4" style="color: black"><em><%= @comentario_carrete.created_at %></em></p>
         </div>
      </div>
          </div>
  </div>
    </div>
        </tr>
</div>
  </div>
</section>

<%= link_to "Volver al listado", comentario_carretes_id_path(@comentario_carrete.carrete.id) %>
<% if current_user == @comentario_carrete.user or current_user.tipo == 'Administrador' %>
<%= link_to "Eliminar comentario", comentario_carrete_path, method: :delete %>
<% end %>
